Markdown is capable of expressing HTML tables. 

## As Structured Data
Markdown Tables can also be used as a way to store [[structured data]]. This format can be converted to other formats such as CSV or JSON using tools like [this](https://tableconvert.com/markdown-to-csv). 